pipline {
agent any
tools { "Maven"

stages {
stage ("initialize"} {
steps {
      sh ***
                  echo "PATH = S{PATH}"
                  echo "M2_HOME)"
                  ***

   }
   Stage { 'Source Composition Analysis'} {
     Stage {
  } 
  stage {'build'} {
  in 'mvn clean package'
  } 
  }
  }
}
