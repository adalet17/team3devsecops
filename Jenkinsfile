pipline {
agent any
tools { "Maven"

stages {
stage ("initialize"} {
steps {
      sh ***
                  echo "PATH = S{PATH}"
                  echo "M2_HOME)"
                  ***

   }
  }
  stage {'build'} {
  in 'mvn clean package'
  } 
  }
  }
}
